## What does this change?

## Why?

## How was this tested?

- [ ] `go build ./...`, `go vet ./...`, `go test ./...` all pass
- [ ] `gofmt -l .` is clean
- [ ] Tested against real WoW data (SavedVariables and/or chat log), if the change touches parsing

## Checklist

- [ ] No secrets (API keys, real `config.json`/`state.json`) added to tracked files
- [ ] Updated `README.md` if config fields, commands, or behavior changed
